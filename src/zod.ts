// Optional Zod adapter — requires 'zod' as a peer dependency.
// Import separately to keep the core bundle free of zod:
//   import { zodCodec } from 'arbitro-ts/zod'
export { zodCodec } from './utils/zod'
